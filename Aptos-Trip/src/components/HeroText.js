// src/components/HeroText.js
import React from 'react';
import './HeroText.css';  // Импортируем CSS файл для стилей

const HeroText = () => (
  <div className="hero-text-container">
    <h1 className="hero-text">
      Aptos Trip - First Travel Show with a New Look at the Value of the NFT
    </h1>
  </div>
);

export default HeroText;
